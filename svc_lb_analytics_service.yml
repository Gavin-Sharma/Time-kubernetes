apiVersion: v1
kind: Service
metadata:
  name: analytics-service
spec:
  type: LoadBalancer
  selector:
    app: analytics-service
  ports:
    - name: analytics-service
      port: 3000
      targetPort: 3000